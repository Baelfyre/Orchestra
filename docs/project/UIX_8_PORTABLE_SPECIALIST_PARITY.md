# UIX-8 Portable Specialist and Adapter Parity

Status: `UIX_8_PORTABLE_SPECIALIST_PARITY_IMPLEMENTED_PENDING_CANONICAL_VALIDATION`

Recorded: 2026-08-24

Entry baseline: `593c7b4444d422f8fb4d78eaf25f2532ffd096ac`

## Purpose

UIX-8 operationalizes the canonical UIX-1 through UIX-7 contracts in the seven UIX-5 owner specialist guidance surfaces. It proves semantic portability for the supported Codex projection without treating parity as host maturity, runtime capability, installation authority, execution authority, or adoption authority.

## Source authority

The UIX-5 specialist integration flow remains the ownership source of truth:

```text
Conductor routes
  -> Cloak captures design and UX evidence
  -> Clockwork defines component, state, responsive, and integration boundaries
  -> The Governor reviews adoption only when proposed
  -> Ponytail implements accepted project-native UI
  -> Cloak repeats static fidelity review
  -> Overseer produces rendered and accessibility evidence
  -> Arbiter owns transition disposition
```

The detailed machine record is `machine/ui/portable-specialist-parity.v1.json`. Its contract inputs are the UIX-1 through UIX-7 phase documents plus the UIX-5 and UIX-6 machine contracts.

## Guidance surfaces

The source `skills/*/SKILL.md` files now state the UIX boundary for Conductor, Cloak, Clockwork, The Governor, Ponytail, Overseer, and Arbiter. The guidance preserves:

- design-source identity, project-native mappings, explicit missing or unresolved evidence, profile invariants, and static-fidelity limits for Cloak;
- frozen-evidence translation into component, state, and responsive architecture without authority transfer for Clockwork;
- conditional adoption review and UIX-6 `NO_ADOPTION` for The Governor;
- frozen upstream contracts, mapped project-native components and tokens, and no arbitrary styling or unapproved substitution for Ponytail;
- exact revision and reference identity, interaction/responsive/accessibility validation, and baseline integrity for Overseer;
- canonical UIX-5 routing and evidence-only boundaries for Conductor; and
- fresh revision-bound evidence and transition ownership for Arbiter.

## Host dispositions

| Host | Maturity | UIX-8 disposition |
| --- | --- | --- |
| Codex | `SUPPORTED` | `NORMALIZED_PORTABLE_COPY` |
| Antigravity | `SUPPORTED` | `NO_COPY_REQUIRED` because no mirrored specialist tree exists |
| Claude Code, Cursor, Windsurf, VS Code/VSCodium, JetBrains, Zed, Neovim | `SCAFFOLD_ONLY` | `SCAFFOLD_ONLY_EXCLUDED` |

UIX-8 does not upgrade host maturity and does not fabricate an Antigravity specialist projection.

## Codex parity

Codex copies are regenerated by `adapters/codex/export-codex-skills.ps1` and checked by `adapters/codex/validate_codex_export.py`. The affected `SKILL.md` surfaces use the existing normalized-body parity rule because Codex frontmatter and portable reference paths legitimately differ from source presentation.

## Validation and authority boundary

`tests/runtime/test_portable_specialist_parity.py` proves the positive Codex projection and rejects stale source copies, authority expansion, scaffold-host promotion, and fabricated Antigravity copy requirements. Strict governance, the existing UIX-1 through UIX-7 deterministic suite, Codex export validation, and full repository behavior validation remain required before canonicalization.

This phase introduces no runtime integration, dependency, external call, installed-integration refresh, Figma mutation, release, deployment, policy activation, or destructive action. Parity is evidence only and grants no host, implementation, execution, installation, or transition authority.

## Exit gate

UIX-8 exits only after the exact source tree passes focused and full validation, a fresh protected source matrix, signed materialization, canonical promotion with an expected-head guard, and independent canonical SHA, tree, parent, and signature readback. Stop before UIX-9 until that readback succeeds.
