name: AQ5 QA Compliance

on:
  workflow_dispatch:
    inputs:
      comparison_base_sha:
        description: Explicit AQ5 comparison baseline commit SHA
        required: true
        type: string
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/qa-compliance.yml
      - CHANGELOG.md
      - README.json
      - docs/architecture/ADAPTIVE_ASSURANCE_AQ5.md
      - machine/adaptive/aq5-qa-compliance.v1.json
      - machine/schemas/qa-compliance.v1.schema.json
      - orchestra_runtime/domain/adaptive/qa_compliance.py
      - scripts/validation/validate_qa_compliance.py
      - tests/behavior/test_qa_compliance.py
      - tests/runtime/test_adaptive_assurance_aq5.py
permissions:
  contents: read

jobs:
  aq5-runtime:
    name: AQ5 runtime contract
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install validation dependencies
        run: |
          python -m pip install --upgrade pip pytest pytest-cov hypothesis==6.163.0 "jsonschema[format]>=4.21,<5"

      - name: Run AQ5 runtime suite
        run: >-
          python -B -m pytest tests/runtime/test_adaptive_assurance_aq5.py
          --cov=orchestra_runtime.domain.adaptive.qa_compliance
          --cov-branch
          --cov-report=term-missing
          --cov-fail-under=97
          -q

      - name: Enforce AQ5 branch coverage
        run: python -B -m coverage report --fail-under=95

  aq5-behavior:
    name: AQ5 CLI behavior
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install behavior dependencies
        run: python -m pip install pytest

      - name: Run AQ5 behavior test
        run: |
          python -B -m pytest tests/behavior/test_qa_compliance.py -q
          python -B tests/behavior/test_qa_compliance.py

  aq5-contract:
    name: AQ5 schema and contract parity
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install schema dependency
        run: python -m pip install --upgrade pip "jsonschema[format]>=4.21,<5"

      - name: Validate AQ5 machine contracts
        run: |
          python -B - <<'PY'
          import json
          from pathlib import Path
          import jsonschema
          from orchestra_runtime.domain.adaptive.qa_compliance import validate_qa_compliance_contract

          root = Path.cwd()
          contract = json.loads((root / "machine/adaptive/aq5-qa-compliance.v1.json").read_text(encoding="utf-8"))
          validate_qa_compliance_contract(contract)
          schema = json.loads((root / "machine/schemas/qa-compliance.v1.schema.json").read_text(encoding="utf-8"))
          jsonschema.Draft202012Validator.check_schema(schema)
          print("AQ5_CONTRACT_PARITY=PASS")
          PY

  aq5-scope:
    name: AQ5 exact scope
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check AQ5 diff scope and whitespace
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          PULL_REQUEST_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          WORKFLOW_DISPATCH_BASE_SHA: ${{ inputs.comparison_base_sha }}
        run: |
          set -euo pipefail
          comparison_base_sha=""
          case "$EVENT_NAME" in
            pull_request)
              comparison_base_sha="$PULL_REQUEST_BASE_SHA"
              ;;
            workflow_dispatch)
              comparison_base_sha="$WORKFLOW_DISPATCH_BASE_SHA"
              ;;
            *)
              echo "Unsupported event for AQ5 comparison scope: $EVENT_NAME" >&2
              exit 1
              ;;
          esac
          if [[ ! "$comparison_base_sha" =~ ^[0-9a-fA-F]{40}$ ]]; then
            echo "AQ5 comparison base SHA is missing or invalid: $comparison_base_sha" >&2
            exit 1
          fi
          comparison_base_sha="${comparison_base_sha,,}"
          current_head="$(git rev-parse HEAD)"
          if [[ "$comparison_base_sha" == "$current_head" ]]; then
            echo "AQ5 comparison base SHA must not equal candidate HEAD" >&2
            exit 1
          fi
          if ! git cat-file -e "${comparison_base_sha}^{commit}" >/dev/null 2>&1; then
            echo "AQ5 comparison base SHA is unavailable locally: $comparison_base_sha" >&2
            exit 1
          fi
          printf "AQ5_COMPARISON_BASE_SHA=%s\n" "$comparison_base_sha"
          git diff --check "$comparison_base_sha..HEAD"
          git diff --name-only "$comparison_base_sha..HEAD" | sort > actual-paths.txt
          scope_applicability="$(python -B scripts/validation/classify_adaptive_assurance_scope.py --assurance aq5 < actual-paths.txt)"
          case "$scope_applicability" in
            NOT_APPLICABLE)
              echo "AQ5_SCOPE=NOT_APPLICABLE"
              exit 0
              ;;
            APPLICABLE)
              ;;
            *)
              echo "AQ5_SCOPE_CLASSIFICATION_INVALID: $scope_applicability" >&2
              exit 1
              ;;
          esac
          cat > aq5-expected-paths.txt <<'EOF'
          .github/workflows/qa-compliance.yml
          CHANGELOG.md
          README.json
          docs/architecture/ADAPTIVE_ASSURANCE_AQ5.md
          machine/adaptive/aq5-qa-compliance.v1.json
          machine/schemas/qa-compliance.v1.schema.json
          orchestra_runtime/domain/adaptive/qa_compliance.py
          scripts/validation/validate_qa_compliance.py
          tests/behavior/test_qa_compliance.py
          tests/runtime/test_adaptive_assurance_aq5.py
          EOF
          cat > prai-expected-paths.txt <<'EOF'
          .github/workflows/prai.yml
          .github/workflows/qa-compliance.yml
          CHANGELOG.md
          README.json
          docs/architecture/ADAPTIVE_ASSURANCE_PRAI.md
          machine/adaptive/prai-post-run-assurance.v1.json
          machine/schemas/prai-post-run-assurance.v1.schema.json
          orchestra_runtime/domain/adaptive/prai.py
          scripts/validation/validate_prai.py
          tests/behavior/test_prai.py
          tests/runtime/test_prai.py
          EOF
          sed -i 's/^          //' aq5-expected-paths.txt prai-expected-paths.txt
          sort -o aq5-expected-paths.txt aq5-expected-paths.txt
          sort -o prai-expected-paths.txt prai-expected-paths.txt
          classify_scope() {
            local actual_file="$1"
            if diff -u aq5-expected-paths.txt "$actual_file" >/dev/null; then
              printf 'AQ5'
              return 0
            fi
            if diff -u prai-expected-paths.txt "$actual_file" >/dev/null; then
              printf 'PRAI_COMPATIBILITY'
              return 0
            fi
            printf 'INVALID'
          }
          scope_classification="$(classify_scope actual-paths.txt)"
          case "$scope_classification" in
            AQ5)
              test "$(wc -l < actual-paths.txt)" -eq 10
              echo "AQ5_SCOPE=PASS"
              ;;
            PRAI_COMPATIBILITY)
              test "$(wc -l < actual-paths.txt)" -eq 11
              echo "AQ5_SCOPE=PRAI_COMPATIBILITY_PASS"
              ;;
            *)
              printf 'Actual paths:\n'
              cat actual-paths.txt
              exit 1
              ;;
          esac
          invalid_fixture="$(mktemp)"
          trap 'rm -f "$invalid_fixture"' EXIT
          {
            grep -v '^README.json$' aq5-expected-paths.txt
            printf '%s\n' unexpected.txt
          } | sort > "$invalid_fixture"
          test "$(classify_scope "$invalid_fixture")" = "INVALID"
          echo "AQ5_SCOPE_NEGATIVE_FIXTURE=PASS"
