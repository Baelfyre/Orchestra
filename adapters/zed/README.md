# Zed Packaging Scaffold

This folder contains scaffold-only Zed packaging files for Orchestra. It points Zed-specific packaging metadata at the shared `ZedAdapter` runtime contract in `orchestra_runtime`.

See [install-guide.md](install-guide.md), [workspace-instructions.template.md](workspace-instructions.template.md), and [package.json](package.json).
