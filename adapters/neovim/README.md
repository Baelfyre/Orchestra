# Neovim Packaging Scaffold

This folder contains scaffold-only Neovim packaging files for Orchestra. It points Neovim-specific packaging metadata at the shared `NeovimAdapter` runtime contract in `orchestra_runtime`.

See [install-guide.md](install-guide.md), [workspace-instructions.template.md](workspace-instructions.template.md), and [package.json](package.json).
