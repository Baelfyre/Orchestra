Implement the Work Queue screen in the supplied project.

Requirements:

1. Reuse existing project-native components when adequate.
2. Use the supplied semantic design tokens.
3. Implement required default, loading, empty, error, and populated states.
4. Preserve supplied asset and reference identities.
5. Support supplied mobile, tablet, and desktop layout constraints.
6. Preserve keyboard operation, semantic structure, labels, focus behavior,
   and supplied accessibility invariants.
7. Do not replace existing components merely to restyle them.
8. Do not introduce new dependencies unless explicitly permitted.
9. Record unresolved mappings rather than silently substituting alternatives.
10. Produce a buildable implementation that satisfies the supplied validator.
