"""Concrete adapters for persistence, hosts, providers, transports, and tooling.

Infrastructure may depend inward on application ports and domain contracts. Core
layers must never depend on these concrete implementations.
"""

__all__: tuple[str, ...] = ()
